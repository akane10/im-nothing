function help() {
  const x = `
  command :
  -add <languages> (to generate gitignore)
  -list (show all list language available)
  -help

  im-nothing <command>
  `;
  console.log(x);
}

module.exports = help;
